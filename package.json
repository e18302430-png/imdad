{
  "name": "imdad-whatsapp-ai-agent",
  "version": "0.1.0",
  "description": "AI agent connected to WhatsApp Cloud API for automated replies.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {},
  "devDependencies": {}
}
